services:
  contract-env:
    build: .
    image: contract-negotiation-env:latest
    ports:
      - "7860:7860"
    environment:
      HOST: "0.0.0.0"
      PORT: "7860"
      HF_TOKEN: "${HF_TOKEN:-}"
